---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: noc
  namespace: kubric
  labels:
    app.kubernetes.io/name: noc
    app.kubernetes.io/part-of: kubric
    kubric/tenant-aware: "true"
  annotations:
    vault.hashicorp.com/agent-inject: "true"
    vault.hashicorp.com/role: "kubric-noc"
---
apiVersion: v1
kind: Service
metadata:
  name: noc
  namespace: kubric
  labels:
    app.kubernetes.io/name: noc
    app.kubernetes.io/part-of: kubric
spec:
  selector:
    app: noc
  ports:
    - name: http
      port: 8083
      targetPort: 8083
    - name: metrics
      port: 9090
      targetPort: 9090
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: noc
  namespace: kubric
  labels:
    app.kubernetes.io/name: noc
    app.kubernetes.io/part-of: kubric
    kubric/tenant-aware: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: noc
  template:
    metadata:
      labels:
        app: noc
        app.kubernetes.io/name: noc
        app.kubernetes.io/part-of: kubric
        kubric/tenant-aware: "true"
    spec:
      serviceAccountName: noc
      containers:
        - name: noc
          image: kubric/noc:latest
          ports:
            - name: http
              containerPort: 8083
            - name: metrics
              containerPort: 9090
          env:
            - name: VAULT_ADDR
              value: http://vault:8200
            - name: VAULT_ROLE
              value: kubric-noc
            - name: NOC_LISTEN_ADDR
              value: ":8083"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8083
            periodSeconds: 30
            initialDelaySeconds: 15
