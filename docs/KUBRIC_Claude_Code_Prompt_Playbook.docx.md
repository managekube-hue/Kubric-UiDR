

| KUBRIC UIDR Claude Code Integration Prompt Playbook *Complete Layer-by-Layer Prompts  •  All 200+ Libraries  •  Cargo.toml  •  go.mod  •  requirements.txt  •  vendor/ setup* How to use:  Paste one prompt at a time into Claude Code in your VS Code terminal. Wait for it to finish and verify before moving to the next prompt. |
| ----- |

# **How to Use This Playbook**

This document contains every Claude Code prompt needed to build the Kubric UIDR monorepo from scratch in your VS Code terminal. Each prompt is self-contained, builds on the previous one, and maps exactly to the 200+ libraries defined in the Kubric architecture document.

| Golden Rule One prompt. Wait for green. Verify. Then next prompt. Never paste two prompts at once. Claude Code needs to see the files it just created before it can wire the next layer. |
| :---- |

Start every Claude Code session with this one-time setup:

| \# Paste this ONCE at the start of every VS Code terminal session export KUBRIC\_DATABASE\_URL="postgresql://postgres.YOUR\_REF:YOUR\_PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres" export VAULT\_ADDR="http://localhost:8200" export NATS\_URL="nats://localhost:4222" \# Then start Claude Code: claude |
| :---- |

| LAYER 0  — Monorepo Scaffold, Proto Schema & Data Backbone | Weeks 1–3 |
| :---- | :---: |

| Layer 0 Goal make build passes. event.proto v1 is locked. NATS flows to ClickHouse with tenant\_id. Supabase schema migrated. Agent enrollment stub works. Feature flags table exists. |
| :---- |

## **PROMPT L0-1 — Create Monorepo Scaffold**

| ▶  CLAUDE CODE PROMPT — L0-1: Monorepo Scaffold |
| :---- |
| Create the Kubric UIDR monorepo scaffold in the current directory. Create this exact directory structure:   kubric/     Cargo.toml          \# Rust workspace     go.work             \# Go workspace     pyproject.toml      \# Python workspace root     Makefile            \# top-level make build target     proto/ocsf/v1/      \# protobuf schemas     agents/       coresec/          \# Rust       netguard/         \# Rust       perftrace/        \# Go (not Rust)     services/       k-svc/            \# Go       vdr/              \# Go       kic/              \# Go       noc/              \# Go     kai/                \# Python package     vendor/             \# data files only \- NOT Go vendor       sigma/rules/       suricata/       yara-rules/       misp/       mitre/       nuclei-templates/       oscal/       velociraptor/       cortex/       falco/       bloodhound/       zeek/       openscap/       wazuh-rules/       osquery/       shuffle-workflows/       coreruleset/       opendlp/       ndpi/lib/     infra/       docker-compose.dev.yml       k8s/       helm/     scripts/            \# kubric-cli (Go Cobra)     docs/runbooks/     db/migrations/      \# SQL migration files Create the Rust workspace Cargo.toml with members \= \['agents/coresec', 'agents/netguard'\]. Create the Go workspace go.work with modules for all four services and scripts/. Create kubric/kai/\_\_init\_\_.py and kubric/kai/pyproject.toml. Create a Makefile with a \`build\` target that runs: cargo build, go build ./..., python \-m py\_compile kai/\_\_init\_\_.py. Run make build and fix any errors until it passes. |

## **PROMPT L0-2 — CoreSec Rust Agent Cargo.toml (Full Library Set)**

This is the complete Cargo.toml for the CoreSec eBPF agent covering all Rust libraries from sections 1 (EDR), 7 (DDR), 23 (Rust Agent Layer).

| ▶  CLAUDE CODE PROMPT — L0-2: CoreSec Cargo.toml — All Rust Libraries |
| :---- |
| Create agents/coresec/Cargo.toml with ALL of the following dependencies. Then run \`cargo build \-p coresec\` and fix any compilation errors. \[package\] name \= "coresec" version \= "0.1.0" edition \= "2021" \[dependencies\] \# \=== ASYNC RUNTIME \=== tokio \= { version \= "1.35", features \= \["full", "signal", "process"\] } \# \=== eBPF (EDR \- section 1\) \=== aya \= "0.12" aya-log \= "0.12" aya-obj \= "0.12" \# \=== MALWARE / YARA (EDR \- section 1\) \=== yara-x \= "0.9" \# \=== SIGMA RULES EVAL (section 29\) \=== \# sigma-rust \= "0.8"   \# LGPL 2.1 \- embed allowed \# \=== EDGE ML INFERENCE (section 23\) \=== candle-core \= { git \= "https://github.com/huggingface/candle.git" } candle-transformers \= { git \= "https://github.com/huggingface/candle.git" } candle-nn \= { git \= "https://github.com/huggingface/candle.git" } tokenizers \= "0.15" \# \=== HASHING (section 23 \+ 24\) \=== blake3 \= "1.5" \# \=== COMPRESSION for OTA updates (section 23\) \=== zstd \= "0.13" \# \=== PROTOBUF for typed NATS payloads (section 30\) \=== prost \= "0.12" prost-types \= "0.12" \# \=== SYSTEM METRICS (section 23\) \=== nix \= "0.27" sysinfo \= "0.30" \# \=== NETWORKING (section 23\) \=== pcap \= "2.0" pnet \= "0.35" \# \=== SIGNAL HANDLING \=== signal-hook \= "0.3" signal-hook-tokio \= { version \= "0.3", features \= \["futures-v0\_3"\] } \# \=== LOGGING & TRACING (section 16 APM) \=== tracing \= "0.1" tracing-subscriber \= "0.3" tracing-opentelemetry \= "0.22" opentelemetry \= "0.21" opentelemetry-otlp \= "0.14" \# \=== CONFIG & SERIALIZATION \=== serde \= { version \= "1.0", features \= \["derive"\] } serde\_yaml \= "0.9" serde\_json \= "1.0" clap \= { version \= "4.4", features \= \["derive", "env"\] } dotenvy \= "0.15" \# \=== PDF REPORTS (section 23\) \=== printpdf \= "0.7" \# \=== FILE WATCHING \=== notify \= "6.1" notify-debouncer-mini \= "0.4" \[build-dependencies\] prost-build \= "0.12" After creating the file, run: cargo build \-p coresec Fix any version conflicts. Do NOT remove any library — find compatible versions. |

## **PROMPT L0-3 — NetGuard Rust Agent Cargo.toml**

NetGuard handles NDR (section 3), NPM (section 13). Includes nDPI FFI and packet processing.

| ▶  CLAUDE CODE PROMPT — L0-3: NetGuard Cargo.toml — NDR Libraries |
| :---- |
| Create agents/netguard/Cargo.toml with these dependencies. Run \`cargo build \-p netguard\` and fix errors. \[package\] name \= "netguard" version \= "0.1.0" edition \= "2021" \[dependencies\] \# Shared with coresec via workspace tokio \= { version \= "1", features \= \["full"\] } serde \= { version \= "1.0", features \= \["derive"\] } prost \= "0.12" blake3 \= "1.5" tracing \= "0.1" tracing-subscriber \= "0.3" \# \=== PACKET CAPTURE (section 3 NDR \+ section 23\) \=== pcap \= "2.0" pnet \= "0.35" \# \=== SYSTEM INFO \=== nix \= "0.27" sysinfo \= "0.30" \# \=== CONFIG \=== clap \= { version \= "4.4", features \= \["derive", "env"\] } dotenvy \= "0.15" serde\_yaml \= "0.9" \# \=== COMPRESSION \=== zstd \= "0.13" \[build-dependencies\] \# For nDPI C FFI (LGPL 3.0 \- FFI link allowed) \# bindgen \= "0.69"   \# Uncomment when vendor/ndpi/lib/libndpi.a is present prost-build \= "0.12" \# NOTE: nDPI (LGPL 3.0) uses FFI \- stub out the bindgen build until \# vendor/ndpi/lib/libndpi.a is compiled from source in Layer 3\. \# RITA (GPL 3.0) runs as SUBPROCESS ONLY \- never import as Cargo dep. Run: cargo build \-p netguard and fix errors. |

## **PROMPT L0-4 — event.proto v1 Schema**

The canonical event schema. Every service, agent, and n8n workflow serializes to this. Lock it before any Go service is built.

| ▶  CLAUDE CODE PROMPT — L0-4: event.proto v1 — Canonical OCSF Schema |
| :---- |
| Create proto/ocsf/v1/event.proto with the complete OCSF event schema. The proto must include ALL of the following message types:   \- OCSFEvent (root wrapper with tenant\_id, event\_id UUIDv7, event\_type enum,     source\_module, severity, ts nanosecond timestamp, payload bytes Any,     mitre\_technique, tags repeated string, schema\_version uint32, blake3\_hash bytes)   \- ProcessActivity (pid, ppid, executable, cmdline, user, group, action enum, exit\_code, env map)   \- NetworkActivity (src\_ip, src\_port, dst\_ip, dst\_port, protocol enum, bytes\_sent,     bytes\_received, packets\_sent, l7\_protocol, beacon\_score, ja3\_hash, sni)   \- FileActivity (path, filename, size, action enum, hash\_blake3, hash\_sha256, user, group, mode)   \- AuthenticationActivity (user, domain, action enum, success bool, src\_ip, auth\_protocol, factors map)   \- VulnerabilityFinding (cve\_id, cvss\_score, severity, epss\_score, in\_kev bool,     package, version, fixed\_version, references, risk\_scores map)   \- ThreatFinding (indicator, type enum, threat\_actor, malware\_family, mitre\_techniques, confidence, source)   \- ComplianceFinding (framework, control\_id, check\_id, status enum, evidence, check\_time)   \- InventoryInfo (asset\_id, hostname, os, os\_version, kernel, cpu, memory\_mb, disks, interfaces, tags, last\_seen)   \- PerformanceMetric (metric\_name, value, labels map, type enum, timestamp) Also create proto/ocsf/v1/billing.proto with:   \- UsageEvent (tenant\_id, module, event\_count, period\_start, period\_end, blake3\_hash) Install buf CLI if not present: npm install \-g @bufbuild/buf Create buf.yaml and buf.gen.yaml to generate Go, Python, and Rust (prost) stubs. Run: buf generate Fix any errors until all three language stubs are generated. The generated Go stubs go to: services/pkg/ocsf/ The generated Python stubs go to: kai/proto/ The Rust stubs are generated via prost-build in the agent build.rs files. CRITICAL: Once this proto is tagged, field numbers may NEVER change. Add a comment at the top: // KUBRIC OCSF EVENT SCHEMA v1 \- FIELD NUMBERS ARE IMMUTABLE |

## **PROMPT L0-5 — Go Workspace go.mod & Core Dependencies**

| ▶  CLAUDE CODE PROMPT — L0-5: Go Workspace — All Layer 0+1 Go Libraries |
| :---- |
| Initialize Go modules for all four services and the scripts CLI. Run these commands: cd services/k-svc && go mod init github.com/kubric/uidr/services/k-svc cd services/vdr && go mod init github.com/kubric/uidr/services/vdr cd services/kic && go mod init github.com/kubric/uidr/services/kic cd services/noc && go mod init github.com/kubric/uidr/services/noc cd scripts && go mod init github.com/kubric/uidr/scripts Then add ALL of these dependencies to EACH service go.mod via go get: \# \=== API FRAMEWORK (section 18, 53\) \=== go get github.com/go-chi/chi/v5 go get github.com/go-chi/cors go get github.com/go-chi/jwtauth/v5 \# \=== NATS MESSAGE BUS (section 18, 19, 27\) \=== go get github.com/nats-io/nats.go \# \=== TEMPORAL WORKFLOW ENGINE (section 18\) \=== go get go.temporal.io/sdk go get go.temporal.io/sdk/client go get go.temporal.io/sdk/worker go get go.temporal.io/sdk/workflow go get go.temporal.io/sdk/activity \# \=== DATABASES (section 18, 26\) \=== go get github.com/ClickHouse/clickhouse-go/v2 go get github.com/jackc/pgx/v5 go get github.com/neo4j/neo4j-go-driver/v5/neo4j go get github.com/marcboeker/go-duckdb \# \=== SECRETS (section 32\) \=== go get github.com/hashicorp/vault/api go get github.com/hashicorp/vault/api/auth/kubernetes \# \=== PROTOBUF (section 30\) \=== go get google.golang.org/protobuf go get google.golang.org/grpc \# \=== OBSERVABILITY (section 16 APM) \=== go get go.opentelemetry.io/otel go get go.opentelemetry.io/otel/trace go get go.opentelemetry.io/otel/metric go get go.opentelemetry.io/otel/sdk go get go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc go get github.com/prometheus/client\_golang/prometheus go get github.com/prometheus/client\_golang/prometheus/promhttp \# \=== LOGGING & TRACING \=== go get go.uber.org/zap go get github.com/jaegertracing/jaeger/model \# \=== CRYPTO / INTEGRITY (section 24\) \=== go get github.com/zeebo/blake3 go get github.com/cbergoon/merkletree \# \=== DATABASE MIGRATIONS (section 24\) \=== go get github.com/golang-migrate/migrate/v4 go get ariga.io/atlas/sql/postgres \# \=== AGENT UPDATE FRAMEWORK (section 24\) \=== go get github.com/theupdateframework/go-tuf/v2 \# \=== STORAGE (section 26\) \=== go get github.com/minio/minio-go/v7 go get github.com/ceph/go-ceph \# \=== MESSAGING ZeroMQ (section 27\) \=== go get github.com/go-zeromq/zmq4 \# \=== SECURITY SCANNING (section 8 VDR, 17 GRC) \=== go get github.com/projectdiscovery/nuclei/v3/pkg/core go get github.com/aquasecurity/trivy/pkg/scanner go get github.com/aquasecurity/trivy/pkg/sbom go get github.com/aquasecurity/kube-bench/check go get github.com/anchore/grype/grype go get github.com/anchore/syft/syft go get github.com/projectdiscovery/subfinder/v2/pkg/runner go get github.com/projectdiscovery/httpx/runner go get github.com/projectdiscovery/naabu/v2/pkg/runner \# \=== COMPLIANCE / POLICY (section 11 CFDR, 17 GRC) \=== go get github.com/open-policy-agent/opa/rego go get github.com/kyverno/kyverno/pkg/engine go get github.com/defenseunicorns/lula go get github.com/ossf/scorecard/v4/checks go get github.com/sigstore/sigstore/pkg/signature \# \=== IDENTITY / BLOODHOUND (section 2 ITDR) \=== go get github.com/SpecterOps/BloodHound/packages/go/analysis go get github.com/neo4j/neo4j-go-driver/v5/neo4j \# \=== CLOUD (section 4 CDR) \=== go get github.com/cloudquery/cloudquery/plugins/source/aws \# \=== EDR FALCO (section 1 EDR) \=== go get github.com/falcosecurity/falco/pkg/engine \# \=== BACKUP / BDR (section 12\) \=== go get github.com/luthermonson/go-proxmox go get github.com/vmware-tanzu/velero/pkg/backup go get github.com/vmware-tanzu/velero/pkg/restore go get github.com/restic/restic/internal/backend go get github.com/restic/restic/internal/checker go get github.com/koacher/kopia/repo \# \=== NPM / PERFORMANCE (section 13 NPM) \=== go get github.com/prometheus/node\_exporter/collector go get github.com/VictoriaMetrics/VictoriaMetrics/app/vmselect go get github.com/VictoriaMetrics/VictoriaMetrics/lib/storage go get github.com/prometheus/blackbox\_exporter/prober go get github.com/prometheus/alertmanager/api go get github.com/grafana/pyroscope-go go get github.com/parca-dev/parca/pkg/profiler \# \=== UEM (section 14\) \=== go get github.com/osquery/osquery-go go get github.com/fleetdm/fleet/v4/server/service \# \=== MDM (section 15\) \=== go get github.com/micromdm/micromdm/mdm go get github.com/micromdm/micromdm/enroll \# \=== API GATEWAY (section 6 ADR) \=== go get github.com/Kong/go-pdk \# \=== K8s CLIENT (section 31\) \=== go get k8s.io/client-go go get github.com/cilium/tetragon/api/v1/tetragon \# \=== DOCKER SDK (section 31\) \=== go get github.com/docker/docker/client \# \=== IaC (section 11 CFDR, 31\) \=== go get github.com/hashicorp/terraform-cdk-go go get github.com/caddy server/caddy/v2 go get github.com/bitnami-labs/sealed-secrets/pkg/client \# \=== BILLING (section 18\) \=== go get github.com/stripe/stripe-go/v76 \# \=== COMMS (section 34\) \=== go get github.com/twilio/twilio-go \# \=== CLI (section 53\) \=== go get github.com/spf13/cobra \# \=== DATA PIPELINE (section 30\) \=== go get github.com/casdoor/casdoor-go-sdk Run: cd services/k-svc && go mod tidy Run for all four services. Fix any import errors. Then run: go build ./... from the workspace root. |

## **PROMPT L0-6 — KAI Python requirements.txt (Full Library Set)**

All Python libraries from sections 18, 25, 26, 27, 28, 45, 46, 47, 48, 49, 50, 51, 52\.

| ▶  CLAUDE CODE PROMPT — L0-6: KAI requirements.txt — All Python Libraries |
| :---- |
| Create kai/requirements.txt with ALL of the following libraries. Then run: pip install \-r kai/requirements.txt \--break-system-packages Fix any version conflicts. Do NOT remove any library. \# \=== CORE AI FRAMEWORK (section 18\) \=== crewai\>=0.28.0 crewai-tools\>=0.1.0 composio-crewai\>=0.3.0 langchain\>=0.1.0 langchain-community\>=0.0.20 langchain-openai\>=0.0.8 langgraph\>=0.0.20 \# \=== TEMPORAL WORKFLOW CLIENT (section 18\) \=== temporalio\>=1.4.0 \# \=== NATS CLIENT (section 18, 19\) \=== nats-py\>=2.6.0 \# \=== LLM INFERENCE (section 28\) \=== vllm\>=0.3.0 llama-cpp-python\>=0.2.50 transformers\>=4.37.0 accelerate\>=0.27.0 bitsandbytes\>=0.42.0 \# \=== LLM CLOUD FALLBACKS (section 50\) \=== openai\>=1.12.0 anthropic\>=0.19.0 cohere\>=5.0.0 \# \=== VECTOR DB / RAG (section 18\) \=== qdrant-client\>=1.7.0 chromadb\>=0.4.22 \# \=== THREAT INTELLIGENCE (section 25\) \=== pymisp\>=2.4.180 stix2\>=3.0.0 stix2-patterns\>=2.0.0 taxii2-client\>=2.3.0 \# \=== EXTERNAL TI APIS (section 36\) \=== shodan\>=1.31.0 censys\>=2.2.0 greynoise\>=2.1.0 \# \=== VULNERABILITY / COMPLIANCE (section 8 VDR, 17 GRC) \=== checkov\>=3.2.0 ssvc\>=0.7.0 compliance-trestle\>=3.3.0 \# \=== DATA PROCESSING (section 25 Python AI) \=== pandas\>=2.2.0 polars\>=0.20.0 numpy\>=1.26.0 pyarrow\>=15.0.0 fastparquet\>=2024.2.0 \# \=== FAST SERIALIZATION (section 45\) \=== orjson\>=3.9.15 ujson\>=5.9.0 msgpack\>=1.0.8 \# \=== ML / AI (section 48 ML) \=== scikit-learn\>=1.4.0 xgboost\>=2.0.3 torch\>=2.2.0 torchvision\>=0.17.0 tensorflow\>=2.16.0 keras\>=3.0.0 ray\[tune\]\>=2.9.0 mlflow\>=2.11.0 shap\>=0.44.0 imbalanced-learn\>=0.12.0 \# \=== PCAP / NETWORK ANALYSIS (section 45\) \=== dpkt\>=1.9.8 pypcap\>=1.3.0 geoip2\>=4.8.0 scapy\>=2.5.0 \# \=== STIX / OSCAL \=== compliance-trestle\>=3.3.0 \# \=== ASYNC API (section 47\) \=== fastapi\>=0.109.0 uvicorn\[standard\]\>=0.27.0 httpx\>=0.26.0 aiohttp\>=3.9.3 \# \=== DATABASE DRIVERS (section 47\) \=== asyncpg\>=0.29.0 clickhouse-connect\>=0.7.0 redis\>=5.0.1 duckdb\>=0.10.0 \# \=== AUTOMATION (section 25, 14 UEM) \=== ansible-runner\>=2.3.4 salt\>=3007.0 pynetbox\>=7.3.3 \# \=== PRESIDIO PII (section 7 DDR) \=== presidio-analyzer\>=2.2.354 presidio-anonymizer\>=2.2.354 spacy\>=3.7.4 \# \=== RISK MODELING (section 17 GRC) \=== pyfair\>=0.1.0 \# \=== TASK QUEUES (section 49\) \=== celery\[redis\]\>=5.3.6 redis\>=5.0.1 \# \=== HTTP / REQUESTS (section 25\) \=== requests\>=2.31.0 aiohttp\>=3.9.3 \# \=== CLI (section 51\) \=== typer\>=0.12.0 click\>=8.1.7 rich\>=13.7.1 tqdm\>=4.66.2 tabulate\>=0.9.0 colorama\>=0.4.6 \# \=== JSON QUERY (section 51\) \=== jsonpath-ng\>=1.6.1 jmespath\>=1.0.1 \# \=== CODE QUALITY (section 44\) \=== ruff\>=0.3.0 mypy\>=1.9.0 bandit\>=1.7.8 safety\>=3.0.1 pytest\>=8.1.0 pytest-asyncio\>=0.23.5 pytest-xdist\>=3.5.0 hypothesis\>=6.100.0 factory-boy\>=3.3.0 faker\>=24.0.0 \# \=== PROTOBUF (section 30\) \=== protobuf\>=4.25.3 grpcio\>=1.62.1 grpcio-tools\>=1.62.1 \# \=== OPENCTI / OSINT (section 10 TI) \=== pycti\>=6.0.0 \# \=== OPENSCAP BINDING (section 11 CFDR, 17 GRC) \=== \# openscap python binding \- install via: apt-get install python3-openscap \# or: pip install openscap-python (unofficial wrapper) |

## **PROMPT L0-7 — Supabase Schema Migration (Layer 0 Tables)**

| ▶  CLAUDE CODE PROMPT — L0-7: Supabase Migration — Layer 0 Core Tables |
| :---- |
| I have a Supabase PostgreSQL database. Connection string is in environment variable KUBRIC\_DATABASE\_URL. Create the following SQL migration file at db/migrations/001\_layer0\_foundation.sql and execute it against Supabase using the connection string. The migration must create ALL of these tables: \-- TENANTS CREATE TABLE tenants (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   name TEXT NOT NULL,   slug TEXT UNIQUE NOT NULL,   subscription\_tier TEXT NOT NULL DEFAULT 'trial',   stripe\_customer\_id TEXT,   created\_at TIMESTAMPTZ NOT NULL DEFAULT now(),   updated\_at TIMESTAMPTZ NOT NULL DEFAULT now() ); \-- FEATURE FLAGS (tenant-scoped module activation) CREATE TABLE feature\_flags (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,   flag\_key VARCHAR(128) NOT NULL,   enabled BOOLEAN NOT NULL DEFAULT false,   rollout\_pct SMALLINT NOT NULL DEFAULT 0 CHECK (rollout\_pct BETWEEN 0 AND 100),   config JSONB,   updated\_at TIMESTAMPTZ NOT NULL DEFAULT now(),   UNIQUE(tenant\_id, flag\_key) ); \-- AGENTS (enrolled CoreSec/NetGuard agents) CREATE TABLE agents (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,   hostname TEXT NOT NULL,   agent\_type TEXT NOT NULL CHECK (agent\_type IN ('coresec','netguard','perftrace')),   version TEXT,   nats\_credential\_id TEXT,   enrolled\_at TIMESTAMPTZ NOT NULL DEFAULT now(),   last\_seen\_at TIMESTAMPTZ,   active BOOLEAN NOT NULL DEFAULT true ); \-- AGENT ENROLLMENT TOKENS (one-time use) CREATE TABLE enrollment\_tokens (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,   token\_hash TEXT NOT NULL UNIQUE,   agent\_type TEXT NOT NULL,   expires\_at TIMESTAMPTZ NOT NULL,   used\_at TIMESTAMPTZ,   created\_by TEXT NOT NULL ); \-- AUDIT LOG (immutable \- no UPDATE/DELETE allowed) CREATE TABLE audit\_log (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID REFERENCES tenants(id),   actor TEXT NOT NULL,   action TEXT NOT NULL,   resource\_type TEXT,   resource\_id TEXT,   payload JSONB,   blake3\_hash TEXT,   ts TIMESTAMPTZ NOT NULL DEFAULT now() ); \-- Prevent modifications to audit log CREATE RULE audit\_no\_update AS ON UPDATE TO audit\_log DO INSTEAD NOTHING; CREATE RULE audit\_no\_delete AS ON DELETE TO audit\_log DO INSTEAD NOTHING; \-- CONTRACTS CREATE TABLE contracts (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,   start\_date DATE NOT NULL,   end\_date DATE,   mrr\_cents INTEGER NOT NULL DEFAULT 0,   modules TEXT\[\] NOT NULL DEFAULT '{}',   status TEXT NOT NULL DEFAULT 'active',   created\_at TIMESTAMPTZ NOT NULL DEFAULT now() ); \-- BILLING USAGE (metered events) CREATE TABLE billing\_usage (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id),   module TEXT NOT NULL,   event\_count BIGINT NOT NULL DEFAULT 0,   period\_start TIMESTAMPTZ NOT NULL,   period\_end TIMESTAMPTZ NOT NULL,   blake3\_hash TEXT,   created\_at TIMESTAMPTZ NOT NULL DEFAULT now() ); \-- BILLING LEDGER (Merkle-chained invoices) CREATE TABLE billing\_ledger (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id),   invoice\_number TEXT UNIQUE NOT NULL,   amount\_cents INTEGER NOT NULL,   stripe\_invoice\_id TEXT,   merkle\_root TEXT,   status TEXT NOT NULL DEFAULT 'draft',   issued\_at TIMESTAMPTZ,   paid\_at TIMESTAMPTZ,   created\_at TIMESTAMPTZ NOT NULL DEFAULT now() ); \-- CASES (MDR \- TheHive schema mapped to Go struct) CREATE TABLE cases (   id UUID PRIMARY KEY DEFAULT gen\_random\_uuid(),   tenant\_id UUID NOT NULL REFERENCES tenants(id),   case\_number TEXT UNIQUE NOT NULL,   title TEXT NOT NULL,   severity TEXT NOT NULL DEFAULT 'medium',   status TEXT NOT NULL DEFAULT 'open',   assigned\_to TEXT,   ocsf\_event\_ids TEXT\[\],   psa\_ticket\_id TEXT,   created\_at TIMESTAMPTZ NOT NULL DEFAULT now(),   updated\_at TIMESTAMPTZ NOT NULL DEFAULT now() ); \-- Create indexes for all foreign keys and common queries CREATE INDEX ON feature\_flags(tenant\_id); CREATE INDEX ON agents(tenant\_id); CREATE INDEX ON agents(tenant\_id, active); CREATE INDEX ON audit\_log(tenant\_id, ts DESC); CREATE INDEX ON billing\_usage(tenant\_id, period\_start); CREATE INDEX ON cases(tenant\_id, status); Use the golang-migrate library to run this migration. Create db/migrations/001\_layer0\_foundation.up.sql with the above SQL. Create db/migrations/001\_layer0\_foundation.down.sql that drops all tables. Create a Go script at scripts/migrate/main.go that runs migrations using   github.com/golang-migrate/migrate/v4 reading KUBRIC\_DATABASE\_URL from env. Run: go run scripts/migrate/main.go Verify all tables exist by querying Supabase. |

## **PROMPT L0-8 — Docker Compose Dev Environment**

| ▶  CLAUDE CODE PROMPT — L0-8: docker-compose.dev.yml — Full Dev Stack |
| :---- |
| Create infra/docker-compose.dev.yml with the complete Kubric dev stack. Include ALL of these services: \# NATS JetStream (message bus \- section 18, 19, 27\) nats:   image: nats:2.10-alpine   command: \["-js", "-m", "8222", "--cluster\_name", "kubric-dev"\]   ports: \["4222:4222", "8222:8222"\] \# ClickHouse (telemetry lake \- section 18\) clickhouse:   image: clickhouse/clickhouse-server:24.1   ports: \["9000:9000", "8123:8123"\]   volumes: \["./data/clickhouse:/var/lib/clickhouse"\] \# Neo4j (identity graph \- section 2 ITDR, 18\) neo4j:   image: neo4j:5.17   ports: \["7474:7474", "7687:7687"\]   environment: {NEO4J\_AUTH: "neo4j/kubric-dev"} \# Temporal (workflow engine \- section 18\) temporal:   image: temporalio/auto-setup:1.24   ports: \["7233:7233"\]   environment: {DB: postgresql, DB\_PORT: 5432}   depends\_on: \[postgres-temporal\] \# PostgreSQL for Temporal (separate from Supabase) postgres-temporal:   image: postgres:16   environment: {POSTGRES\_PASSWORD: temporal, POSTGRES\_USER: temporal, POSTGRES\_DB: temporal} \# Redis (cache \+ Celery broker \- section 49\) redis:   image: redis:7.2-alpine   ports: \["6379:6379"\] \# Vault dev mode (secrets \- section 32\) vault:   image: hashicorp/vault:1.15   command: server \-dev \-dev-root-token-id=kubric-dev-token   ports: \["8200:8200"\]   environment: {VAULT\_DEV\_ROOT\_TOKEN\_ID: kubric-dev-token} \# Qdrant (vector DB for RAG \- section 18\) qdrant:   image: qdrant/qdrant:v1.8.0   ports: \["6333:6333"\] \# Ollama (local LLM \- section 28\) ollama:   image: ollama/ollama:latest   ports: \["11434:11434"\]   volumes: \["ollama\_models:/root/.ollama"\] \# n8n (workflow automation sidecar \- section 27\) n8n:   image: n8nio/n8n:latest   ports: \["5678:5678"\]   environment:     N8N\_BASIC\_AUTH\_ACTIVE: "true"     N8N\_BASIC\_AUTH\_USER: kubric     N8N\_BASIC\_AUTH\_PASSWORD: kubric-dev     VAULT\_ADDR: http://vault:8200   depends\_on: \[vault\] \# MinIO (S3-compatible object storage \- backup/PCAP \- section 12\) minio:   image: minio/minio:latest   command: server /data \--console-address :9001   ports: \["9002:9000", "9001:9001"\]   environment: {MINIO\_ROOT\_USER: kubric, MINIO\_ROOT\_PASSWORD: kubric-dev} \# Grafana (observability UI \- section 41\) grafana:   image: grafana/grafana:10.3.0   ports: \["3000:3000"\] \# Prometheus (metrics \- section 41\) prometheus:   image: prom/prometheus:v2.50.0   ports: \["9090:9090"\] \# Loki (log aggregation \- section 41\) loki:   image: grafana/loki:2.9.5   ports: \["3100:3100"\] volumes:   ollama\_models: Then run: docker-compose \-f infra/docker-compose.dev.yml up \-d Wait for all containers to be healthy. Verify NATS is reachable: curl http://localhost:8222/varz Verify ClickHouse is reachable: curl http://localhost:8123/ping Verify Vault is unsealed: curl http://localhost:8200/v1/sys/health |

## **PROMPT L0-9 — ClickHouse Schema (Tenant-Partitioned Event Tables)**

| ▶  CLAUDE CODE PROMPT — L0-9: ClickHouse — OCSF Event Tables with tenant\_id |
| :---- |
| Connect to ClickHouse at localhost:8123 and create the following tables. CRITICAL: Every table must be partitioned by (tenant\_id, toYYYYMM(ts)). Use the clickhouse-go/v2 driver or run via HTTP API. \-- RAW EVENTS (all OCSF events, 90-day TTL default) CREATE TABLE kubric.events (   tenant\_id UUID NOT NULL,   event\_id UUID NOT NULL,   event\_type LowCardinality(String) NOT NULL,   source\_module LowCardinality(String) NOT NULL,   severity LowCardinality(String),   ts DateTime64(9, 'UTC') NOT NULL,   payload String,   mitre\_technique String,   tags Array(String),   blake3\_hash FixedString(32),   schema\_version UInt32 DEFAULT 1 ) ENGINE \= MergeTree() PARTITION BY (tenant\_id, toYYYYMM(ts)) ORDER BY (tenant\_id, ts, event\_id) TTL ts \+ INTERVAL 90 DAY DELETE; \-- VULNERABILITY FINDINGS (7-year retention for compliance) CREATE TABLE kubric.vuln\_findings (   tenant\_id UUID NOT NULL,   finding\_id UUID NOT NULL,   cve\_id String,   cvss\_score Float32,   epss\_score Float32,   in\_kev UInt8 DEFAULT 0,   severity LowCardinality(String),   asset\_id String,   package String,   version String,   fixed\_version String,   ts DateTime64(3) NOT NULL,   suppressed UInt8 DEFAULT 0 ) ENGINE \= MergeTree() PARTITION BY (tenant\_id, toYYYYMM(ts)) ORDER BY (tenant\_id, cve\_id, asset\_id) TTL ts \+ INTERVAL 7 YEAR DELETE; \-- THREAT INTELLIGENCE (TI feeds \- section 10\) CREATE TABLE kubric.threat\_intel (   indicator String NOT NULL,   indicator\_type LowCardinality(String),   source LowCardinality(String),   confidence Float32,   threat\_actor String,   malware\_family String,   mitre\_techniques Array(String),   first\_seen DateTime,   last\_seen DateTime,   ts DateTime DEFAULT now() ) ENGINE \= MergeTree() PARTITION BY toYYYYMM(ts) ORDER BY (indicator, source) TTL ts \+ INTERVAL 30 DAY DELETE; \-- COMPLIANCE FINDINGS (7-year retention) CREATE TABLE kubric.compliance\_findings (   tenant\_id UUID NOT NULL,   framework LowCardinality(String),   control\_id String,   status LowCardinality(String),   evidence String,   check\_time DateTime NOT NULL,   ts DateTime DEFAULT now() ) ENGINE \= MergeTree() PARTITION BY (tenant\_id, toYYYYMM(ts)) ORDER BY (tenant\_id, framework, control\_id) TTL ts \+ INTERVAL 7 YEAR DELETE; \-- AGENT METRICS (2-year retention) CREATE TABLE kubric.agent\_metrics (   tenant\_id UUID NOT NULL,   agent\_id UUID NOT NULL,   metric\_name LowCardinality(String),   value Float64,   labels Map(String, String),   ts DateTime64(3) NOT NULL ) ENGINE \= MergeTree() PARTITION BY (tenant\_id, toYYYYMM(ts)) ORDER BY (tenant\_id, agent\_id, metric\_name, ts) TTL ts \+ INTERVAL 2 YEAR DELETE; Verify all tables exist: SELECT name, engine FROM system.tables WHERE database='kubric'; |

| LAYER 1  — Platform Core APIs — Go Services | Weeks 4–7 |
| :---- | :---: |

| Layer 1 Goal All four Go services return 200 on health checks. Vault wired. Temporal running. Feature flags CRUD working. Purge API functional. All routes tenant-scoped via JWT. |
| :---- |

## **PROMPT L1-1 — K-SVC (Customer/Tenant API)**

| ▶  CLAUDE CODE PROMPT — L1-1: Build K-SVC — Tenant, Enrollment, Feature Flags |
| :---- |
| Build services/k-svc/ as a Go chi HTTP service. Use these packages (already in go.mod):   github.com/go-chi/chi/v5   github.com/go-chi/cors   github.com/go-chi/jwtauth/v5   github.com/jackc/pgx/v5  (connect to KUBRIC\_DATABASE\_URL)   github.com/nats-io/nats.go   github.com/hashicorp/vault/api   go.temporal.io/sdk/client   github.com/stripe/stripe-go/v76   github.com/zeebo/blake3   go.uber.org/zap   go.opentelemetry.io/otel/trace Build these routes: POST /v1/tenants                          — create tenant GET  /v1/tenants/:id                      — get tenant GET  /v1/tenants/:id/flags                — list feature flags PUT  /v1/tenants/:id/flags/:key           — set feature flag POST /v1/agents/enroll                    — enroll agent (validates enrollment token) GET  /v1/agents/:id/renew                 — renew NATS credentials GET  /v1/agents/tenant/:tenant\_id         — list agents for tenant DELETE /admin/tenants/:id/purge           — delete ALL data for tenant GET  /healthz                             — health check Requirements:   \- Every route except /healthz requires Authorization: Bearer JWT   \- JWT must contain tenant\_id claim — extract it with jwtauth   \- Routes must reject requests where URL tenant\_id \!= JWT tenant\_id   \- /admin/tenants/:id/purge writes to audit\_log BEFORE deleting any data   \- All secrets fetched from Vault at startup, NOT from env vars   \- All DB operations use pgx and the Supabase connection string   \- Publish audit events to NATS subject: kubric.{tenant\_id}.audit.{action}   \- OpenTelemetry spans on every handler Run: go build ./services/k-svc/... and fix all errors. Run: go test ./services/k-svc/... and fix any test failures. Run the service: go run services/k-svc/main.go Test: curl http://localhost:8080/healthz — must return 200 {status: ok} |

## **PROMPT L1-2 — VDR, KIC, NOC Services**

| ▶  CLAUDE CODE PROMPT — L1-2: Build VDR \+ KIC \+ NOC Services |
| :---- |
| Build these three Go services following the same pattern as K-SVC. Each must use chi, jwtauth, pgx, NATS, Vault, OTel. VDR (services/vdr/) — Vulnerability Detection & Response   Libraries: nuclei/v3/pkg/core, trivy/pkg/scanner, trivy/pkg/sbom, anchore/syft/syft,              anchore/grype/grype, projectdiscovery/subfinder, httpx, naabu   Routes:     POST /v1/scans               — trigger nuclei/trivy scan for a target     GET  /v1/findings/:tenant\_id — list CVE findings for tenant     POST /v1/findings/:id/suppress — suppress a finding     GET  /v1/sbom/:asset\_id      — get SBOM for an asset     GET  /healthz KIC (services/kic/) — Compliance & Configuration   Libraries: open-policy-agent/opa/rego, kyverno/kyverno/pkg/engine,              ossf/scorecard, sigstore/sigstore, defenseunicorns/lula,              aquasecurity/kube-bench   Routes:     GET  /v1/controls/:framework  — list OSCAL controls for framework     POST /v1/assessments          — run compliance assessment     GET  /v1/gaps/:tenant\_id      — list compliance gaps     POST /v1/policy/eval          — evaluate OPA policy against input     GET  /healthz NOC (services/noc/) — Operations & Alerting   Libraries: prometheus/client\_golang, prometheus/alertmanager/api,              VictoriaMetrics, grafana/pyroscope-go, luthermonson/go-proxmox,              vmware-tanzu/velero, restic, koacher/kopia, minio/minio-go   Routes:     POST /v1/alerts              — create alert     PUT  /v1/alerts/:id/status   — update alert status     GET  /v1/alerts/:tenant\_id   — list alerts     GET  /v1/sla/:tenant\_id      — SLA metrics     POST /v1/backup/trigger      — trigger backup job     GET  /healthz Run all three: go build ./services/... && fix all errors. Test each health endpoint returns 200\. |

| LAYER 2  — AI & Automation Layer — KAI \+ n8n \+ Temporal | Weeks 8–12 |
| :---- | :---: |

| Layer 2 Goal CrewAI personas receive real NATS events via Temporal workflows. n8n has Vault-managed credentials. TRIAGE agent classifies a synthetic alert end-to-end. |
| :---- |

## **PROMPT L2-1 — KAI NATS Consumer \+ CrewAI Personas**

| ▶  CLAUDE CODE PROMPT — L2-1: Build KAI — NATS Consumer \+ All 5 CrewAI Personas |
| :---- |
| Build the kai/ Python package with the following structure and ALL libraries wired. kai/   \_\_init\_\_.py   nats\_consumer.py     — async NATS subscriber, routes events to agents   agents/     triage.py          — KAI-TRIAGE persona     sentinel.py        — KAI-SENTINEL persona     keeper.py          — KAI-KEEPER persona     comm.py            — KAI-COMM persona     foresight.py       — KAI-FORESIGHT persona   workflows/     alert\_triage.py    — Temporal workflow     scan\_enrichment.py — Temporal workflow     incident\_response.py — Temporal workflow   chains/     enrichment.py      — LangChain alert enrichment chain     ti\_context.py      — LangChain TI context retrieval   api/     main.py            — FastAPI model-serving endpoints Requirements for nats\_consumer.py:   \- Use nats-py async client   \- Subscribe to: kubric.\*.edr.\>, kubric.\*.ndr.\>, kubric.\*.itdr.\>   \- CRITICAL: extract tenant\_id from NATS subject (position 1\)   \- Route to correct agent by event\_type   \- All secrets fetched from Vault (VAULT\_ADDR env var) via hvac Requirements for each CrewAI agent:   \- Import: from crewai import Agent, Task, Crew   \- TRIAGE: classify severity, deduplicate, MITRE ATT\&CK mapping,             write enriched finding back to NOC API via httpx   \- SENTINEL: cross-event correlation, threat hunting on NATS stream   \- KEEPER: orchestrate response via Cortex responders as subprocess,             use ansible\_runner for remediation playbooks   \- COMM: draft customer alerts and PSA tickets using Ollama REST,           POST http://localhost:11434/api/generate   \- FORESIGHT: risk forecasting using PyFAIR, EPSS scores from ClickHouse,                produce KiSS scorecard component Requirements for Temporal workflows:   \- Import: from temporalio import activity, workflow   \- alert\_triage workflow: receive OCSFEvent → TRIAGE agent → write to ClickHouse   \- Every workflow has: retry\_policy MaxAttempts=3, schedule\_to\_close\_timeout=10min   \- Terminal failure → escalation\_activity posts to NOC API Test: python \-c 'from kai.agents.triage import TriageAgent; print(TriageAgent())' Fix any import errors. |

## **PROMPT L2-2 — n8n Vault Integration \+ Cloud Workflows**

| ▶  CLAUDE CODE PROMPT — L2-2: Configure n8n with Vault-Managed Credentials |
| :---- |
| n8n is running at http://localhost:5678 via docker-compose. Vault is running at http://localhost:8200 with token kubric-dev-token. Do the following: 1\. Store all cloud API credentials in Vault:    vault kv put secret/kubric/n8n/o365 client\_id=PLACEHOLDER client\_secret=PLACEHOLDER tenant\_id=PLACEHOLDER    vault kv put secret/kubric/n8n/google\_workspace client\_id=PLACEHOLDER client\_secret=PLACEHOLDER    vault kv put secret/kubric/n8n/aws access\_key\_id=PLACEHOLDER secret\_access\_key=PLACEHOLDER 2\. Create a Vault policy that allows n8n to read n8n credentials:    path "secret/data/kubric/n8n/\*" { capabilities \= \["read"\] } 3\. Create a shell script at scripts/n8n-vault-init.sh that:    \- Reads all n8n secrets from Vault using the vault CLI    \- Sets them as n8n environment variables via n8n API    \- NEVER writes secrets to disk or any file 4\. Create n8n workflow JSON files at infra/n8n/workflows/:    \- o365\_event\_polling.json  — polls Microsoft Graph API every 5min,                                posts events to NOC API POST /v1/alerts    \- ti\_feed\_polling.json     — polls AbuseIPDB \+ OTX every 15min,                                inserts to ClickHouse threat\_intel table    \- cisa\_kev\_sync.json       — polls CISA KEV JSON daily,                                POSTs to VDR API to trigger re-scoring 5\. Verify: none of the n8n workflow files contain any plaintext secrets.    All credential references must use n8n credential IDs, not values. Run scripts/n8n-vault-init.sh and verify workflows load in n8n UI. |

| LAYER 3  — Detection & Intelligence — Rules, Feeds, Scanning | Weeks 13–18 |
| :---- | :---: |

| Layer 3 Goal Sigma rules evaluate. YARA scans. RITA runs as subprocess (GPL boundary enforced). All 6 TI feeds populate ClickHouse. Nuclei/Trivy/Grype feed VDR. Every module gated by feature flag. |
| :---- |

## **PROMPT L3-1 — Vendor Directory: Pull All Detection Data Files**

| ▶  CLAUDE CODE PROMPT — L3-1: Pull All Vendored Detection Rules & Intelligence Data |
| :---- |
| Populate the vendor/ directory by cloning/downloading the following. These are DATA FILES only — no code is imported from GPL/AGPL sources. Run each command and verify the directory is populated: \# SIGMA RULES (Apache 2.0 \- copy freely) git clone \--depth=1 https://github.com/SigmaHQ/sigma vendor/sigma echo 'Sigma rules count:' && find vendor/sigma/rules \-name '\*.yml' | wc \-l \# SURICATA ET RULES (GPL 2.0 \- rules as data files, no code import) mkdir \-p vendor/suricata \# Download Emerging Threats rules (free ruleset) curl \-L https://rules.emergingthreats.net/open/suricata-7.0/emerging.rules.tar.gz \-o /tmp/et.tar.gz tar \-xzf /tmp/et.tar.gz \-C vendor/suricata/ echo 'Suricata rules count:' && find vendor/suricata \-name '\*.rules' | wc \-l \# YARA RULES (mixed licenses \- data files) git clone \--depth=1 https://github.com/Yara-Rules/rules vendor/yara-rules echo 'YARA rules count:' && find vendor/yara-rules \-name '\*.yar' | wc \-l \# MISP TAXONOMIES (CC0 \- embed freely) git clone \--depth=1 https://github.com/MISP/misp-taxonomies vendor/misp/taxonomies git clone \--depth=1 https://github.com/MISP/misp-galaxy vendor/misp/galaxies git clone \--depth=1 https://github.com/MISP/misp-warninglists vendor/misp/warninglists git clone \--depth=1 https://github.com/MISP/misp-objects vendor/misp/objects \# MITRE ATT\&CK STIX (CC BY 4.0 \- embed) mkdir \-p vendor/mitre curl \-L https://raw.githubusercontent.com/mitre/cti/master/enterprise-attack/enterprise-attack.json \-o vendor/mitre/enterprise-attack.json curl \-L https://raw.githubusercontent.com/mitre/cti/master/ics-attack/ics-attack.json \-o vendor/mitre/ics-attack.json \# NUCLEI TEMPLATES (MIT \- copy freely) git clone \--depth=1 https://github.com/projectdiscovery/nuclei-templates vendor/nuclei-templates echo 'Nuclei templates count:' && find vendor/nuclei-templates \-name '\*.yaml' | wc \-l \# NIST OSCAL (Public Domain) mkdir \-p vendor/oscal curl \-L https://raw.githubusercontent.com/usnistgov/oscal-content/main/nist.gov/SP800-53/rev5/json/NIST\_SP-800-53\_rev5\_catalog.json \-o vendor/oscal/nist-sp800-53-rev5.json \# FALCO RULES (Apache 2.0 \- data files) git clone \--depth=1 https://github.com/falcosecurity/rules vendor/falco/rules \# BLOODHOUND CYPHER QUERIES (Apache 2.0 \- copy freely) git clone \--depth=1 https://github.com/SpecterOps/BloodHound vendor/bloodhound-src mkdir \-p vendor/bloodhound/cypher find vendor/bloodhound-src \-name '\*.cypher' \-exec cp {} vendor/bloodhound/cypher/ \\; rm \-rf vendor/bloodhound-src \# ZEEK SCRIPTS (BSD-3 \- copy scripts) git clone \--depth=1 https://github.com/zeek/zeek vendor/zeek-src cp \-r vendor/zeek-src/scripts vendor/zeek/ rm \-rf vendor/zeek-src \# OSQUERY PACKS (Apache 2.0 \- copy) git clone \--depth=1 https://github.com/osquery/osquery vendor/osquery-src cp \-r vendor/osquery-src/packs vendor/osquery/ rm \-rf vendor/osquery-src \# MODSECURITY CRS (Apache 2.0 \- embed rules) git clone \--depth=1 https://github.com/coreruleset/coreruleset vendor/coreruleset \# VELOCIRAPTOR ARTIFACTS (AGPL 3.0 \- load as data only) git clone \--depth=1 https://github.com/Velocidex/velociraptor vendor/velociraptor-src cp \-r vendor/velociraptor-src/artifacts vendor/velociraptor/ rm \-rf vendor/velociraptor-src \# NOTE: AGPL 3.0 \- these YAML files are loaded as data at runtime, NOT imported as code \# CORTEX ANALYZERS/RESPONDERS (AGPL 3.0 \- exec as subprocess only) git clone \--depth=1 https://github.com/TheHive-Project/Cortex-Analyzers vendor/cortex-src cp \-r vendor/cortex-src/analyzers vendor/cortex/ cp \-r vendor/cortex-src/responders vendor/cortex/ rm \-rf vendor/cortex-src \# NOTE: AGPL 3.0 \- these Python scripts are exec'd as subprocesses, NEVER imported Print final summary of all vendor directories and file counts. |

## **PROMPT L3-2 — Wire Sigma-Rust \+ YARA-X into CoreSec**

| ▶  CLAUDE CODE PROMPT — L3-2: Wire sigma-rust \+ YARA-X into CoreSec Rust Agent |
| :---- |
| Wire the Sigma and YARA detection engines into the CoreSec Rust agent. 1\. Add sigma-rust to agents/coresec/Cargo.toml:    sigma-rust \= "0.8"  \# LGPL 2.1 \- embed allowed 2\. Create agents/coresec/src/detection/sigma.rs that:    \- Loads ALL YAML files from vendor/sigma/rules/ at startup    \- Compiles them into a sigma\_rust RuleSet    \- Exposes fn evaluate\_sigma(event: \&OCSFEvent) \-\> Vec\<SigmaMatch\>    \- Returns match results with rule\_id, rule\_title, severity    \- Must handle 25,000+ rules without panicking 3\. Create agents/coresec/src/detection/yara.rs that:    \- Loads ALL .yar files from vendor/yara-rules/ at startup using yara-x    \- Exposes fn scan\_yara(data: &\[u8\]) \-\> Vec\<YaraMatch\>    \- Returns match results with rule\_name, namespace, tags 4\. Create agents/coresec/src/detection/mod.rs that combines both:    \- pub struct DetectionEngine { sigma: SigmaEngine, yara: YaraEngine }    \- pub fn detect(\&self, event: \&OCSFEvent, data: Option\<&\[u8\]\>) \-\> DetectionResults 5\. Write a test in agents/coresec/src/detection/tests.rs:    \- Load a synthetic process event    \- Run through DetectionEngine    \- Assert at least 1 Sigma rule loads without panic    \- Assert YARA engine initializes without panic Run: cargo test \-p coresec detection and fix all errors. |

## **PROMPT L3-3 — RITA Subprocess (GPL Boundary)**

RITA is GPL 3.0. It must run as a subprocess. This prompt enforces the boundary.

| GPL 3.0 HARD BOUNDARY RITA must NEVER appear in go.mod. It runs as a subprocess communicating via loopback gRPC or stdin/stdout. Violation contaminates the entire Go binary with GPL 3.0. |
| :---- |

| ▶  CLAUDE CODE PROMPT — L3-3: RITA Subprocess Wrapper (GPL 3.0 Boundary) |
| :---- |
| Implement RITA as a subprocess sidecar — NOT a Go import. RITA is GPL 3.0. Do NOT add it to go.mod. Do NOT import any RITA package. 1\. Create a Docker sidecar config at infra/docker-compose.dev.yml — add:    rita:      image: activecm/rita:v5.0      ports: \["4000:4000"\]      volumes: \["./data/zeek:/data/zeek", "./data/rita:/data/rita"\]      command: \["--zeek-dir", "/data/zeek", "--serve", ":4000"\] 2\. Create services/netguard/rita/client.go that:    \- Communicates with RITA via HTTP on localhost:4000    \- Exposes type RITAClient struct { baseURL string }    \- func (c \*RITAClient) GetBeacons(tenant\_id string) (\[\]Beacon, error)    \- func (c \*RITAClient) GetDNSTunneling(tenant\_id string) (\[\]DNSTunnel, error)    \- func (c \*RITAClient) GetExfil(tenant\_id string) (\[\]ExfilEvent, error)    \- All responses deserialized from JSON into local Go structs    \- ZERO imports from github.com/activecm/rita — only net/http and encoding/json 3\. Add a comment block at the top of rita/client.go:    // KUBRIC LICENSE BOUNDARY: RITA is GPL 3.0.    // This file communicates with RITA via HTTP only.    // No RITA package may ever be imported into this binary.    // Verified: grep \-r 'activecm/rita' services/ must return 0 results. 4\. Add a Makefile target \`check-gpl-boundary\` that runs:    grep \-r 'activecm/rita' services/ && echo 'GPL VIOLATION DETECTED' && exit 1 || echo 'GPL boundary clean' Run: make check-gpl-boundary — must output 'GPL boundary clean' Run: go build ./services/netguard/... and fix errors. |

## **PROMPT L3-4 — TI Feed Ingestion Pipeline**

| ▶  CLAUDE CODE PROMPT — L3-4: Wire All 6 TI Feeds into ClickHouse |
| :---- |
| Create kai/intel/ti\_feeds.py that ingests all threat intelligence feeds. Implement async feed pullers for ALL of these (use httpx for async HTTP): 1\. NVD CVE Feed (hourly):    GET https://services.nvd.nist.gov/rest/json/cves/2.0?resultsPerPage=2000    → Insert to ClickHouse kubric.vuln\_findings 2\. CISA KEV (daily):    GET https://www.cisa.gov/sites/default/files/feeds/known\_exploited\_vulnerabilities.json    → Insert to ClickHouse, mark in\_kev=1, publish NATS kubric.kev.updated 3\. FIRST EPSS (daily):    GET https://epss.cyentia.com/epss\_scores-current.csv.gz    → Parse CSV with polars, insert epss\_score to ClickHouse vuln\_findings 4\. AlienVault OTX (15min):    GET https://otx.alienvault.com/api/v1/pulses/subscribed (with X-OTX-API-KEY header)    → Insert indicators to ClickHouse kubric.threat\_intel    → API key fetched from Vault: secret/kubric/ti/otx 5\. AbuseIPDB (30min):    GET https://api.abuseipdb.com/api/v2/blacklist (with Key header)    → Insert IP blocklist to ClickHouse kubric.threat\_intel    → API key from Vault: secret/kubric/ti/abuseipdb 6\. IPSum (1hr):    GET https://raw.githubusercontent.com/stamparm/ipsum/master/ipsum.txt    → Parse TXT, insert to ClickHouse kubric.threat\_intel, source='ipsum' 7\. PyMISP feed ingestion (1hr):    Use pymisp to connect to a MISP instance (MISP\_URL from Vault)    → Ingest events and attributes to ClickHouse kubric.threat\_intel Requirements:   \- All API keys fetched from Vault (hvac client), NEVER hardcoded   \- Use clickhouse-connect for bulk async inserts   \- Each feed wrapped in try/except with structured logging (structlog or loguru)   \- Each feed returns ingestion count and stores last\_run in Redis   \- Use asyncio.gather() to run all feeds concurrently Create kai/intel/scheduler.py using APScheduler to run feeds on schedule. Test: python \-c 'from kai.intel.ti\_feeds import TIFeedManager; print(TIFeedManager())' |

| LAYER 4  — Production Infrastructure — K8s, GitOps, DR, Observability | Weeks 19–24 |
| :---- | :---: |

| Layer 4 Goal K8s on Proxmox running. ArgoCD deploys on git push. Backup restore drill done. Platform alerting live. All runbooks written. On-call rostered before customer 1\. |
| :---- |

## **PROMPT L4-1 — Kubernetes Manifests & Helm Charts**

| ▶  CLAUDE CODE PROMPT — L4-1: K8s Manifests for All Kubric Services |
| :---- |
| Create K8s manifests in infra/k8s/base/ for all Kubric services. Create Deployment \+ Service \+ ServiceAccount for each:   \- k-svc (port 8080\)   \- vdr (port 8081\)   \- kic (port 8082\)   \- noc (port 8083\)   \- kai-core (port 8100\)   \- n8n (port 5678\) Each Deployment must have:   \- resource limits: cpu 500m/1000m, memory 512Mi/1Gi   \- readinessProbe: GET /healthz, initialDelaySeconds: 10   \- livenessProbe: GET /healthz, periodSeconds: 30   \- env var VAULT\_ADDR pointing to vault service   \- NO secrets in env vars — all secrets via External Secrets Operator   \- label: app.kubernetes.io/part-of: kubric   \- label: kubric/tenant-aware: 'true' Create infra/k8s/base/kustomization.yaml listing all resources. Create infra/k8s/overlays/dev/kustomization.yaml with:   \- replica count: 1 for all services   \- image tag: dev Create infra/k8s/overlays/prod/kustomization.yaml with:   \- replica count: 3 for k-svc, 2 for others   \- image tag: latest Create infra/helm/values/   nats-values.yaml      — NATS JetStream cluster, 3 replicas   clickhouse-values.yaml — ClickHouse cluster   vault-values.yaml     — Vault HA with Raft storage   temporal-values.yaml  — Temporal cluster   grafana-values.yaml   — Grafana with ClickHouse datasource Run: kubectl apply \-k infra/k8s/overlays/dev/ \--dry-run=client Fix any manifest errors. |

## **PROMPT L4-2 — Grafana Alerting Rules (Platform Observability)**

| ▶  CLAUDE CODE PROMPT — L4-2: Platform Alerting — 6 Required Grafana Alert Rules |
| :---- |
| Create Grafana alert rules that fire BEFORE any customer alert fires. These are NON-NEGOTIABLE — platform must alert on its own failures. Create infra/grafana/alerts/platform-alerts.json with rules for: 1\. NATS Consumer Lag \> 10,000 messages:    expr: nats\_consumer\_num\_pending \> 10000    severity: critical    annotations: summary='NATS consumer lag critical \- events not being processed' 2\. ClickHouse Insert Rate \= 0 for \> 2min:    expr: rate(clickhouse\_written\_rows\_total\[2m\]) \== 0 AND on() kubric:events:total \> 0    severity: critical    annotations: summary='ClickHouse ingestion stopped' 3\. Vault Sealed:    expr: vault\_core\_unsealed \== 0    severity: critical    annotations: summary='VAULT IS SEALED \- all services will fail secret lookups' 4\. Agent Heartbeat Gap \> 5min:    expr: time() \- kubric\_agent\_last\_seen\_timestamp\_seconds \> 300    severity: warning    annotations: summary='Agent {{ $labels.agent\_id }} has not reported in 5min' 5\. Temporal Workflow Failure Rate \> 5%:    expr: rate(temporal\_workflow\_failed\_total\[5m\]) / rate(temporal\_workflow\_completed\_total\[5m\]) \> 0.05    severity: critical    annotations: summary='Temporal workflow failure rate exceeds 5%' 6\. Service Pod CrashLoopBackOff:    expr: kube\_pod\_container\_status\_waiting\_reason{reason='CrashLoopBackOff', namespace='kubric'} \> 0    severity: critical    annotations: summary='Pod {{ $labels.pod }} is in CrashLoopBackOff' Also create infra/grafana/dashboards/   kubric-platform.json   — platform health dashboard   kubric-customer.json   — per-tenant alert and compliance dashboard Create a Grafana provisioning ConfigMap at infra/k8s/base/grafana-provisioning.yaml that mounts the dashboards and alert rules at Grafana startup. Run kubectl apply \--dry-run=client on the ConfigMap and fix any errors. |

## **PROMPT L4-3 — Backup & Disaster Recovery Scripts**

| ▶  CLAUDE CODE PROMPT — L4-3: DR Scripts — ClickHouse \+ Vault \+ Postgres \+ Neo4j Backup |
| :---- |
| Create backup scripts at scripts/backup/ using the libraries in go.mod. 1\. scripts/backup/clickhouse.go:    \- Uses clickhouse-go/v2 to connect to ClickHouse    \- Runs clickhouse-backup create for each partition older than 1 day    \- Uploads backup to MinIO using minio-go/v7    \- Bucket: kubric-backups, path: clickhouse/{date}/    \- Verifies upload by listing MinIO objects    \- Writes audit log entry to Supabase (KUBRIC\_DATABASE\_URL) 2\. scripts/backup/vault.go:    \- Uses vault/api to create a Vault Raft snapshot    \- Uploads snapshot to MinIO: vault/{timestamp}/vault.snap    \- Runs every 5 minutes via cron 3\. scripts/backup/postgres.go:    \- Uses pgx/v5 to connect to Supabase    \- Runs pg\_dump via exec.Command    \- Uploads dump to MinIO: postgres/{date}/kubric.dump 4\. scripts/backup/neo4j.go:    \- Uses exec.Command to run neo4j-admin database dump    \- Uploads to MinIO: neo4j/{date}/kubric.neo4j 5\. scripts/backup/restore\_test.go:    \- A Go test that:      a) Triggers a ClickHouse backup      b) Lists the backup from MinIO      c) Restores it to a temp ClickHouse table      d) Verifies row count matches original    \- This is the RESTORE DRILL test — must pass before customer 1 Create a Makefile target \`restore-drill\` that runs: go test ./scripts/backup/... \-run TestRestoreDrill \-v Run go build ./scripts/backup/... and fix all errors. |

| LAYER 5  — Customer-Facing — Portal, Billing, PSA, Auth | Weeks 25–30 |
| :---- | :---: |

| Layer 5 Goal Customers log in, see only their data, pay via Stripe. KiSS scorecard visible. PSA tickets auto-created. Pen test clean before any customer is onboarded. |
| :---- |

## **PROMPT L5-1 — Next.js Customer Portal**

| ▶  CLAUDE CODE PROMPT — L5-1: Next.js Portal — Full Library Setup \+ KiSS Dashboard |
| :---- |
| Create the customer portal at frontend/ using Next.js 14 App Router. Install ALL of these packages:   npm install next@14 react react-dom typescript @types/react @types/node   npm install @tremor/react         \# analytics dashboard components (Apache 2.0)   npx shadcn@latest init            \# Shadcn/UI components (MIT)   npx shadcn@latest add button dialog table sheet badge card   npm install lucide-react          \# security icons (ISC)   npm install tailwindcss postcss autoprefixer   npm install @supabase/supabase-js \# Supabase auth client   npm install stripe                \# Stripe billing   npm install swr                   \# data fetching   npm install nats.ws               \# NATS WebSocket for realtime alerts Create these pages: app/layout.tsx               — root layout with Authentik OIDC session app/(auth)/login/page.tsx    — login page app/(tenant)/dashboard/page.tsx — main alert dashboard with Tremor AreaChart app/(tenant)/vulns/page.tsx  — vulnerability findings with Tremor Table app/(tenant)/compliance/page.tsx — compliance gaps by framework app/(tenant)/kiss/page.tsx   — KiSS scorecard (5 domain score cards) app/(tenant)/agents/page.tsx — enrolled agent health status app/(tenant)/billing/page.tsx — Stripe billing portal link Requirements:   \- ALL pages extract tenant\_id from JWT session — never trust URL param alone   \- ALL API calls include Authorization: Bearer {jwt}   \- KiSS scorecard page shows: Identity, Endpoint, Network, Cloud, Compliance scores     each as a Tremor DonutChart \+ Metric card   \- Dashboard has NATS WebSocket connection for realtime alert feed   \- Agent health page shows: last\_seen\_at, active status, agent\_type per agent Run: npm run build from frontend/ directory Fix any TypeScript errors. Run: npm run dev and verify dashboard loads at http://localhost:3000 |

## **PROMPT L5-2 — Stripe Billing Integration**

| ▶  CLAUDE CODE PROMPT — L5-2: Stripe Billing — K-SVC Billing Endpoints |
| :---- |
| Wire Stripe billing into K-SVC using github.com/stripe/stripe-go/v76. Create services/k-svc/billing/ package with: 1\. billing/stripe.go:    \- Fetch Stripe secret key from Vault: secret/kubric/stripe/secret\_key    \- func CreateCustomer(tenant \*Tenant) (\*stripe.Customer, error)    \- func CreateSubscription(customerId string, tier string) (\*stripe.Subscription, error)    \- func CreateUsageRecord(subscriptionItemId string, quantity int64) error    \- func GetBillingPortalURL(customerId string) (string, error) 2\. billing/metering.go:    \- Subscribe to NATS subject: kubric.\*.billing.usage.\>    \- For each UsageEvent: insert to Supabase billing\_usage table    \- At end of billing period: aggregate usage, create Stripe usage record    \- Generate Merkle root from billing\_usage rows using cbergoon/merkletree    \- Store merkle\_root in billing\_ledger table 3\. Add K-SVC routes:    POST /v1/billing/checkout    — create Stripe checkout session    POST /v1/billing/portal      — get Stripe billing portal URL    POST /v1/billing/webhook     — handle Stripe webhook events    GET  /v1/billing/usage/:id   — get usage summary for tenant 4\. Stripe webhook handling:    \- customer.subscription.created → update tenant.subscription\_tier    \- customer.subscription.deleted → downgrade to 'trial', disable premium flags    \- invoice.paid → mark billing\_ledger entry as paid Run go build ./services/k-svc/... and fix errors. Write a test: TestBillingMerkleRoot that creates 3 usage records and verifies the Merkle root is deterministic. |

## **PROMPT L5-3 — PSA Integration (Auto-Ticket Creation)**

| ▶  CLAUDE CODE PROMPT — L5-3: PSA Tickets via KAI-COMM — Zammad REST |
| :---- |
| Wire KAI-COMM to automatically create PSA tickets for critical alerts. 1\. Create kai/psa/zammad.py:    \- Fetch Zammad URL and API token from Vault: secret/kubric/psa/zammad    \- async def create\_ticket(tenant\_id, title, body, priority) \-\> ticket\_id    \- async def update\_ticket(ticket\_id, status) \-\> bool    \- Idempotency: check if ticket exists for event\_id before creating      (store event\_id → ticket\_id mapping in Redis) 2\. Update kai/agents/comm.py to call create\_ticket for:    \- CRITICAL severity alerts    \- Compliance framework failures    \- Agent offline \> 5min 3\. Create kai/reports/qbr.py:    \- Generates monthly QBR PDF for a tenant    \- Uses FORESIGHT persona to summarize risk trends    \- Uses COMM persona to draft executive summary    \- Renders PDF using reportlab    \- Uploads PDF to MinIO: kubric-reports/{tenant\_id}/{year}-{month}-qbr.pdf    \- Creates Zammad ticket linking to the PDF Test: python \-c 'from kai.psa.zammad import ZammadClient; print(ZammadClient())' Write kai/tests/test\_psa.py with a mock Zammad test that verifies idempotency (calling create\_ticket twice with same event\_id creates only 1 ticket). |

# **Master Library Quick Reference**

Complete mapping of all 200+ libraries to their file, prompt, and layer for quick lookup.

## **Rust Libraries → Cargo.toml (Prompt L0-2, L0-3)**

| Library | Import | Layer | Purpose |
| ----- | ----- | ----- | ----- |
| **aya / aya-log** | aya \= "0.12" | **L0 CoreSec** | eBPF kernel events \- EDR process monitoring |
| **yara-x** | yara-x \= "0.9" | **L0 CoreSec** | YARA malware scanning \- BSd-3, direct embed |
| **sigma-rust** | sigma-rust \= "0.8" | **L3 CoreSec** | Sigma rule eval in Rust \- LGPL 2.1 |
| **candle-core/nn/transformers** | git \= candle repo | **L0 CoreSec** | Edge ML inference \- Phi-3/TinyLlama on endpoint |
| **blake3** | blake3 \= "1.5" | **L0 All** | BLAKE3 hashing \- immutable audit chain |
| **zstd** | zstd \= "0.13" | **L0 All** | Compression for OTA updates \+ NATS payloads |
| **prost / prost-types** | prost \= "0.12" | **L0 All** | Protobuf serialization for typed NATS events |
| **tokio** | tokio \= { version="1", features=\["full"\] } | **L0 All** | Async runtime for all Rust agents |
| **nix** | nix \= "0.27" | **L0 PerfTrace** | POSIX syscalls for host metrics |
| **sysinfo** | sysinfo \= "0.30" | **L0 PerfTrace** | CPU/RAM/disk metrics \- cross-platform |
| **pcap** | pcap \= "2.0" | **L0 NetGuard** | libpcap packet capture \- forensic recording |
| **pnet** | pnet \= "0.35" | **L0 NetGuard** | AF\_PACKET V3 zero-copy ring buffer |
| **printpdf** | printpdf \= "0.7" | **L0 CoreSec** | PDF reports from Rust agent |
| **signal-hook** | signal-hook \= "0.3" | **L0 All** | Graceful shutdown signal handling |
| **notify** | notify \= "6.1" | **L0 CoreSec** | File watching for hot rule reload |
| **tracing \+ otel** | tracing \= "0.1" | **L0 All** | Structured logging \+ OTel spans |

## **Go Libraries → go.mod (Prompt L0-5)**

| Library | Import | Layer | Purpose |
| ----- | ----- | ----- | ----- |
| **go-chi/chi/v5** | github.com/go-chi/chi/v5 | **L1 All Services** | HTTP router \- all four Go services |
| **go-chi/cors** | github.com/go-chi/cors | **L1 K-API** | CORS for Next.js portal cross-origin |
| **go-chi/jwtauth/v5** | github.com/go-chi/jwtauth/v5 | **L1 All Services** | JWT auth middleware \- tenant\_id extraction |
| **nats-io/nats.go** | github.com/nats-io/nats.go | **L0 All** | NATS JetStream message bus client |
| **temporal Go SDK** | go.temporal.io/sdk | **L1 K-SVC** | Durable workflow engine client |
| **clickhouse-go/v2** | github.com/ClickHouse/clickhouse-go/v2 | **L0 K-DATA** | ClickHouse telemetry lake driver |
| **pgx/v5** | github.com/jackc/pgx/v5 | **L1 K-SVC** | Supabase/Postgres driver |
| **neo4j-go-driver/v5** | github.com/neo4j/neo4j-go-driver/v5/neo4j | **L2 KAI** | Identity graph \- BloodHound schema |
| **go-duckdb** | github.com/marcboeker/go-duckdb | **L2 CoreSec** | Embedded analytics in agent |
| **vault/api** | github.com/hashicorp/vault/api | **L1 All** | Secrets management \- no plaintext env vars |
| **nuclei/v3/pkg/core** | github.com/projectdiscovery/nuclei/v3/pkg/core | **L3 VDR** | Vulnerability scanner \- MIT embed |
| **trivy/pkg/scanner** | github.com/aquasecurity/trivy/pkg/scanner | **L3 VDR** | Container/IaC vuln scanner \- Apache 2.0 |
| **trivy/pkg/sbom** | github.com/aquasecurity/trivy/pkg/sbom | **L3 VDR** | SBOM generation and parsing |
| **anchore/syft/syft** | github.com/anchore/syft/syft | **L3 VDR** | SBOM generator (Syft) |
| **anchore/grype/grype** | github.com/anchore/grype/grype | **L3 VDR** | CVE matcher against SBOM |
| **open-policy-agent/opa/rego** | github.com/open-policy-agent/opa/rego | **L3 KIC** | Policy-as-code evaluation |
| **kyverno/kyverno/pkg/engine** | github.com/kyverno/kyverno/pkg/engine | **L3 KIC** | K8s admission policy |
| **SpecterOps/BloodHound go/analysis** | github.com/SpecterOps/BloodHound/packages/go/analysis | **L2 KAI** | AD attack path analysis \- Apache 2.0 |
| **cloudquery/cloudquery** | github.com/cloudquery/cloudquery/plugins/source/aws | **L3 NOC** | Cloud asset discovery \- CDR module |
| **falcosecurity/falco/pkg/engine** | github.com/falcosecurity/falco/pkg/engine | **L1 CoreSec** | Process monitoring engine \- Apache 2.0 |
| **stripe-go/v76** | github.com/stripe/stripe-go/v76 | **L5 K-SVC** | Stripe billing SDK |
| **twilio-go** | github.com/twilio/twilio-go | **L5 KAI-COMM** | SMS/voice alerts for critical incidents |
| **spf13/cobra** | github.com/spf13/cobra | **L0 CLI** | kubric-cli subcommand framework |
| **zeebo/blake3 (Go)** | github.com/zeebo/blake3 | **L1 K-DATA** | Go BLAKE3 \- cross-verified with Rust layer |
| **cbergoon/merkletree** | github.com/cbergoon/merkletree | **L5 K-SVC** | Billing ledger Merkle root |
| **golang-migrate/migrate/v4** | github.com/golang-migrate/migrate/v4 | **L0 K-DATA** | DB schema versioning \- Supabase migrations |
| **go-tuf/v2** | github.com/theupdateframework/go-tuf/v2 | **L4 NOC** | Signed agent OTA update framework |
| **minio/minio-go/v7** | github.com/minio/minio-go/v7 | **L4 NOC** | Object storage \- PCAP/backups/reports |
| **velero** | github.com/vmware-tanzu/velero/pkg/backup | **L4 NOC** | K8s backup \- BDR module |
| **restic** | github.com/restic/restic/internal/backend | **L4 NOC** | File backups \- BDR module |
| **prometheus/client\_golang** | github.com/prometheus/client\_golang/prometheus | **L4 NOC** | Platform metrics exposition |
| **grafana/pyroscope-go** | github.com/grafana/pyroscope-go | **L4 APM** | eBPF continuous profiling |
| **go-zeromq/zmq4** | github.com/go-zeromq/zmq4 | **L2 CoreSec** | Sub-ms intra-host IPC between agents |
| **ossf/scorecard/v4** | github.com/ossf/scorecard/v4/checks | **L3 KIC** | Supply chain security scoring |
| **sigstore/sigstore** | github.com/sigstore/sigstore/pkg/signature | **L3 KIC** | Container image signing |

## **Python Libraries → requirements.txt (Prompt L0-6)**

| Library | Import | Layer | Purpose |
| ----- | ----- | ----- | ----- |
| **crewai** | pip install crewai | **L2 KAI** | Multi-agent orchestration framework |
| **composio-crewai** | pip install composio-crewai | **L2 KAI** | 200+ tool integrations for CrewAI agents |
| **langchain \+ langgraph** | pip install langchain langgraph | **L2 KAI** | LLM chains for alert enrichment |
| **temporalio** | pip install temporalio | **L2 KAI** | Temporal workflow Python SDK |
| **nats-py** | pip install nats-py | **L2 KAI** | Async NATS consumer for KAI |
| **vllm** | pip install vllm | **L2 KAI** | High-throughput LLM serving (70B models) |
| **llama-cpp-python** | pip install llama-cpp-python | **L2 KAI** | CPU LLM inference \- air-gapped fallback |
| **openai / anthropic / cohere** | pip install openai anthropic cohere | **L2 KAI** | Cloud LLM fallbacks (cost/sovereignty aware) |
| **pymisp** | pip install pymisp | **L3 KAI** | MISP API client for IOC ingestion |
| **stix2 \+ stix2-patterns** | pip install stix2 stix2-patterns | **L3 KAI** | STIX 2.1 threat intel objects |
| **shodan / censys / greynoise** | pip install shodan censys greynoise | **L3 KIC** | TPRM and EAS intelligence |
| **checkov** | pip install checkov | **L3 VDR** | IaC security scanning \- Apache 2.0 |
| **ssvc** | pip install ssvc | **L3 VDR** | SSVC decision tree risk scoring |
| **compliance-trestle** | pip install compliance-trestle | **L3 KIC** | OSCAL authoring and validation |
| **pandas / polars** | pip install pandas polars | **L2 KAI** | Data processing for ML and EPSS feeds |
| **orjson / msgpack** | pip install orjson msgpack | **L2 KAI** | Fast serialization for NATS payloads |
| **scikit-learn / xgboost** | pip install scikit-learn xgboost | **L2 KAI** | Risk scoring and anomaly detection models |
| **torch / tensorflow** | pip install torch tensorflow | **L2 KAI** | Deep learning for traffic classification |
| **ray\[tune\]** | pip install ray\[tune\] | **L2 KAI** | Distributed ML training and tuning |
| **mlflow** | pip install mlflow | **L2 KAI** | ML experiment tracking and model registry |
| **presidio-analyzer** | pip install presidio-analyzer | **L3 CoreSec** | PII detection \- DDR module |
| **pyfair** | pip install pyfair | **L2 KAI** | FAIR risk quantification model |
| **dpkt / scapy** | pip install dpkt scapy | **L3 KAI** | PCAP analysis for threat hunting |
| **geoip2** | pip install geoip2 | **L3 KAI** | IP geolocation \- impossible travel detection |
| **fastapi \+ uvicorn** | pip install fastapi uvicorn | **L2 KAI** | KAI model-serving API |
| **asyncpg** | pip install asyncpg | **L2 KAI** | Async Postgres \- fastest Python PG driver |
| **clickhouse-connect** | pip install clickhouse-connect | **L2 KAI** | ClickHouse bulk insert for TI feeds |
| **ansible-runner** | pip install ansible-runner | **L2 KAI-KEEPER** | Remediation playbook execution |
| **typer / rich / click** | pip install typer rich click | **L0 CLI** | kubric-kai CLI framework |
| **pytest \+ hypothesis \+ factory-boy** | pip install pytest hypothesis factory-boy | **L0 DevOps** | Test infrastructure |

## **Vendor Data Files (GPL/AGPL/CC — Prompt L3-1)**

| Source | License | vendor/ path | Module |
| ----- | ----- | ----- | ----- |
| **Sigma Rules (25,000+ YAML)** | Apache 2.0 | vendor/sigma/ | CoreSec ITDR / NetGuard |
| **Suricata ET Rules (5,000+)** | GPL 2.0 — data | vendor/suricata/ | NetGuard IDS/C2/Malware/WAF |
| **YARA Rules (5,000+)** | Mixed — data | vendor/yara-rules/ | CoreSec Scanner DDR |
| **MISP Taxonomies/Galaxies** | CC0 — embed free | vendor/misp/ | KAI Context / SIDR TI |
| **MITRE ATT\&CK STIX** | CC BY 4.0 | vendor/mitre/ | KAI Intel / CoreSec |
| **Nuclei Templates (MIT)** | MIT — copy free | vendor/nuclei-templates/ | VDR Scanner Cloud API |
| **NIST OSCAL (Public Domain)** | Public Domain | vendor/oscal/ | KIC Compliance GRC |
| **Falco Rules (Apache 2.0)** | Apache 2.0 | vendor/falco/ | CoreSec Container K8s |
| **BloodHound Cypher (Apache 2.0)** | Apache 2.0 | vendor/bloodhound/ | KAI Identity ITDR |
| **Zeek Scripts (BSD-3)** | BSD-3 | vendor/zeek/ | NetGuard TLS DNS HTTP |
| **Osquery Packs (Apache 2.0)** | Apache 2.0 | vendor/osquery/ | CoreSec FIM UEM |
| **ModSecurity CRS (Apache 2.0)** | Apache 2.0 | vendor/coreruleset/ | NetGuard WAF ADR |
| **Velociraptor Artifacts (AGPL)** | AGPL — data only | vendor/velociraptor/ | CoreSec Forensics |
| **Cortex Analyzers/Responders (AGPL)** | AGPL — subprocess | vendor/cortex/ | KAI Keeper MDR |
| **Wazuh Rules (GPL 2.0 — data)** | GPL 2.0 — data | vendor/wazuh-rules/ | CoreSec EDR ITDR |

| Final Reminder — How Claude Code Works Best Each prompt above is one task. Paste it, wait for Claude Code to finish, check the output, then move to the next. If Claude Code gets stuck or makes an error, just say 'that failed, here is the error' and paste the error. It will fix it. Never skip the verify step at the end of each prompt. |
| :---- |

KUBRIC UIDR — Claude Code Prompt Playbook v1.0 — Confidential